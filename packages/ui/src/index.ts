// Shared UI components — populated as we build the web app.
// Each component here is available to both apps/web and apps/architecture.
export { useVoiceInput } from './hooks/useVoiceInput'
export type { UseVoiceInputReturn } from './hooks/useVoiceInput'
